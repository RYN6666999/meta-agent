# Novel Analyzer MCP Server — 依賴清單
mcp[cli]>=1.0.0
httpx>=0.27.0

# ChromaDB 向量搜尋（optional，用於 search_scenes 工具）
chromadb>=0.5.0
sentence-transformers>=2.7.0

# 其他已有依賴（server.py 已有，這邊列出供獨立安裝用）
fastapi>=0.110.0
uvicorn>=0.29.0
sqlalchemy>=2.0
