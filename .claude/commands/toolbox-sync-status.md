---
description: Sync machine-readable toolbox status files into CRM public assets for UI refresh.
---

Run:

```bash
bash /Users/ryan/meta-agent/scripts/toolbox/sync-status-to-ui.sh
```

Then refresh the toolbox UI page.
