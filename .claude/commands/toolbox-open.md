---
description: Open toolbox desktop launcher app (fallback to command file).
---

Run:

```bash
bash /Users/ryan/meta-agent/scripts/toolbox/toolbox-open.sh
```

If open fails, report the missing launcher and how to recreate it.
